Generated code:
# Query: open everywhere except 1cm around the cap.
gripper_map = get_empty_gripper_map()
# open everywhere
gripper_map[:, :, :] = 1
# close when 1cm around the cap
cap = parse_query_obj('cap')
set_voxel_by_radius(gripper_map, cap.position, radius_cm=1, value=0)
ret_val = gripper_map
